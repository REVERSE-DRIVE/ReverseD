public enum PartType
{
    Axis,
    Body,
    Leg,
}

public enum AxisType
{
    Normal,
    Light,
    Heavy,
}

public enum BodyType
{
    Normal,
    Light,
    Heavy,
}

public enum LegType
{
    Normal,
    Light,
    Heavy,
}